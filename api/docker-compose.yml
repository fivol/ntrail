version: "3.3"
services:
  api:
    build: .
    restart: always
    ports:
      - "8000:8000"
    depends_on:
      - redis
    env_file:
      - env_file

  redis:
    build: redis
    restart: always
    ports:
      - "6379:6379"